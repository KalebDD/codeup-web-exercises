"use strict";

//Narcissistic Numbers - output first 25

//NN = non-negative integer with m digits where the sum of all the individual digits raised to the power m is equal to n.

//EX: 1^3 + 5^3 + 3^3 = 1 + 125 + 27 = 153

function narcissisticNumbers() {
    var counter = 0;

    while (counter <= 25) {
        var sum = [];

        // for (var i = 0; i < 10; i++) {
        //     if (Math.pow(i, i.toString().length) === i) {
        //         sum = i;
        //         console.log(sum);
        //     }
        // }
        for (var i = 0; i < Infinity; i++) {
            var length = i.toString().length;
            var numberIndex = 0;
            if (Math.pow())

        }
    }
}